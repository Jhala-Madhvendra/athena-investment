# Copy this file to .env (or .env.production) and fill in real values.
# Vite only exposes variables prefixed with VITE_, and it bundles them into
# the client-side JS bundle — never put secrets here, only public config.

VITE_API_URL=http://localhost:5000
